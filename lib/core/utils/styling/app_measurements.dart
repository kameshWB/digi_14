class AppMeasures {
  static double pageMargin = 40;

  static double screenWidth = 0;
  static double screenHeight = 0;

  /// Initiates the ZuzuMeasures class to hold the [screenHeight] and [screenWidth].
  static initiate(double height, double width) {
    screenHeight = height;
    screenWidth = width;
  }
}
